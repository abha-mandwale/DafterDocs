# DafterDocs

`dafterdocs/` is a parallel conversion of the existing DafterDocs project from Expo/React Native into:

- `FastAPI`
- `Jinja2`
- `htmx`
- `Tailwind CSS`
- `PostgreSQL`

The new implementation lives entirely under [dafterdocs/backend](backend).
